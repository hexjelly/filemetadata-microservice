import test from 'ava';

test('/: returns 200 and html', t => {
	t.fail();
});

test('/api/: valid data returns 200 and json', t => {
	t.fail();
});

test('/api/: invalid data returns 400 and json with error', t => {
	t.fail();
});

test('/api/: wrong field(s) returns 400 and json with error', t => {
	t.fail();
});
